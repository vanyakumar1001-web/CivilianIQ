# Base URL for the deployed Supabase Edge Functions backend.
# Deploy the functions in /supabase (see supabase/README.md), then set this to:
#   https://<your-project-ref>.supabase.co/functions/v1
VITE_SUPABASE_FUNCTIONS_URL=
